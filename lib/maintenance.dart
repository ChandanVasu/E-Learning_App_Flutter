import 'package:flutter/material.dart';

class maintenance   extends StatelessWidget {
  const maintenance  ({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Image.asset("assets/maintenance.png")),
    );
  }
}
